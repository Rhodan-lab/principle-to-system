name: Publish Phase 47 candidate

on:
  pull_request:
    branches: [main]
    paths:
      - "scripts/publish_phase47_candidate.py"
      - ".github/workflows/publish-phase-47-candidate.yml"

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-47-offline-authorization-decision-candidate-population-execution-readiness
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Publish deterministic Phase 47 package
        env:
          PYTHONDONTWRITEBYTECODE: "1"
        run: |
          if [ ! -f scripts/publish_phase47_candidate.py ]; then
            echo "Publisher already removed."
            exit 0
          fi
          python3 scripts/publish_phase47_candidate.py
